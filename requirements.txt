# Runtime dependencies.
# Versions are pinned because the models are saved as pickles: an unpinned
# scikit-learn can fail to unpickle them after a minor version bump.
numpy==2.4.6
pandas==3.0.3
scikit-learn==1.8.0
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
