# Render blueprint. Create the service once via Render's dashboard
# ("New > Blueprint"); pushes to main deploy automatically after that.
services:
  - type: web
    name: healthscanner
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile

    autoDeploy: true

    healthCheckPath: /healthz
